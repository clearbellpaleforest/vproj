# Vim 8.2 Excluded Test Files
# These files use Neovim-only APIs (vim.api.nvim_*) extensively and
# cannot run on Vim 8.2, especially in headless mode.
# The Vim 8.2 test runner should skip these entirely.
# One file per line, leading whitespace stripped, # lines treated as comments.

tests/spec/edge_cases_spec.lua
tests/spec/outline_mode_spec.lua
tests/spec/sidebar_spec.lua
tests/spec/stress_spec.lua
